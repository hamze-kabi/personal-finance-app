- in budgets/page.tsx in the following line:
  import { Budget, Transaction } from "@/types";
  under Budget and Transaction there is red line saying
  Module '"@/types"' has no exported member 'Budget'.
  Module '"@/types"' has no exported member 'Transaction'.

- in recurring-bills/page.tsx in the following line:
  import { Transaction } from "@/types";
  under Transaction there is a red line saying:
  Module '"@/types"' has no exported member 'Transaction'.
